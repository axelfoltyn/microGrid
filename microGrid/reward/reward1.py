def benef(param, price_buy, price_sell=None):
    if price_vente is None:
        price_sell = price_buy
    return param[vente] * price_sell - param[achat] * price_buy

def f2(param):
    res =0
    if #utilisation de batterie
    if #achat dՎnergie
    if #pas assez dՎnergie
    return res

def f3(param, L_load_use, L_load_cost):
    for i in range(len(L_load_use)):
        res += param[L_load_use[i]] * L_load_cost[i]
    return res